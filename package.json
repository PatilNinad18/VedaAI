{
  "name": "veda-ai",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["backend", "frontend", "shared"],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm --workspace=backend run dev",
    "dev:frontend": "npm --workspace=frontend run dev",
    "build": "npm --workspace=shared run build && npm --workspace=backend run build && npm --workspace=frontend run build",
    "install:all": "npm install && npm --workspace=backend install && npm --workspace=frontend install && npm --workspace=shared install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
